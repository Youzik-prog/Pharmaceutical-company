import { ProgressPercentagePipe } from './progress-percentage-pipe';

describe('ProgressPercentagePipe', () => {
  it('create an instance', () => {
    const pipe = new ProgressPercentagePipe();
    expect(pipe).toBeTruthy();
  });
});
