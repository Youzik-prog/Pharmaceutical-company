import { MultiProgressDirective } from './multi-progress.directive';

describe('MultiProgress', () => {
  it('should create an instance', () => {
    const directive = new MultiProgressDirective();
    expect(directive).toBeTruthy();
  });
});
